run:
linters:
  enable:
    - nestif
    - funlen
    - lll
    - forcetypeassert
    - gofumpt
    - govet
    - staticcheck
    - revive
    - errorlint
    - exportloopref
    - cyclop
    - unparam
    - unused
    - errcheck

linters-settings:
  funlen:
    ignore-comments: true
    lines: 100

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - funlen
        - cyclop
        - lll
